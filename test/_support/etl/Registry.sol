// SPDX-License-Identifier: MIT
pragma solidity ^0.8.29;

contract Ops {

    // /*//////////////////////////////////////////////////////////////
    //                         BASIC OPS
    // //////////////////////////////////////////////////////////////*/

    // function _add(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a + b;
    // }

    // function _mul(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a * b;
    // }

    // function _sub(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a - b;
    // }

    // /*//////////////////////////////////////////////////////////////
    //                         COMPARISONS
    // //////////////////////////////////////////////////////////////*/

    // function _gt(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a > b ? 1 : 0;
    // }

    // function _lt(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a < b ? 1 : 0;
    // }

    // function _eq(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a == b ? 1 : 0;
    // }

    // /*//////////////////////////////////////////////////////////////
    //                         BITWISE OPS
    // //////////////////////////////////////////////////////////////*/

    // function _and(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a & b;
    // }

    // function _or(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a | b;
    // }

    // function _shl(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a << b;
    // }

    // function _shr(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a >> b;
    // }

    // /*//////////////////////////////////////////////////////////////
    //                         STRUCT OPS (examples)
    // //////////////////////////////////////////////////////////////*/

    // function _diff(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a > b ? a - b : b - a;
    // }

    // /*//////////////////////////////////////////////////////////////
    //                     ARRAY GENERATION OPS
    // //////////////////////////////////////////////////////////////*/

    // function _expand(uint256 a, uint256 b, uint256 i) external pure returns (uint256) {
    //     // deterministic pseudo expansion
    //     return uint256(keccak256(abi.encode(a, b, i)));
    // }

    // function _sum(uint256 a, uint256 b, uint256) external pure returns (uint256) {
    //     return a + b;
    // }

    // /*//////////////////////////////////////////////////////////////
    //                     ARRAY RETURN OPS (for flatMap)
    // //////////////////////////////////////////////////////////////*/

    // function _split(uint256 a, uint256, uint256) external pure returns (uint256[] memory out) {
    //     out = new uint256[](2);
    //     out[0] = a;
    //     out[1] = a / 2;
    // }

    // function _dup(uint256 a, uint256, uint256) external pure returns (uint256[] memory out) {
    //     out = new uint256[](2);
    //     out[0] = a;
    //     out[1] = a;
    // }
}